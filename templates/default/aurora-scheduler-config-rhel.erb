!/bin/bash
# This file has been generated by Chef.

# Global options
<% @glob_opts.sort.each do |key, value| %>
<%= key %>=<%= value %>
<% end %>

# Flags that control the behavior of the JVM.
JAVA_OPTS=(
<% @java_opts.sort.each do |flag| %>
  <%= flag %>
<% end %>
)

# Flags control the behavior of the Aurora scheduler.
# For a full list of available flags, run /usr/lib/aurora/bin/aurora-scheduler -help
AURORA_FLAGS=(
<% @app_config.sort.each do |key, value| %>
  -<%= key %>='<%= value %>'
<% end %>
)
